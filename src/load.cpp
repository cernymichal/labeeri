#include "load.h"

GLint loadSingleShader(const char* path, GLenum shaderType) {
    // TODO
    return -1;
}

namespace load {

Shader shader(const char* vertexPath, const char* fragmentPath) {
    // TODO
    return Shader(-1);
}
Mesh mesh(const char* filePath) {
    // TODO
    return Mesh(-1, -1, -1, 0);
}

}  // namespace load
